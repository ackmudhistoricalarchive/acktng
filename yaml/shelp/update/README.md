# yaml/shelp/update/

Patches to existing skill/spell help entries (shelp). Include `filename` plus only
the fields to change.

Applied by:
    python3 tools/yaml_apply.py yaml/shelp/update/berserk.yaml

The file is deleted after successful application.
