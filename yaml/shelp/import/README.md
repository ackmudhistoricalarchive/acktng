# yaml/shelp/import/

New skill/spell help entries (shelp) to INSERT into the database. One file per entry.

Applied by:
    python3 tools/yaml_apply.py yaml/shelp/import/berserk.yaml

The file is deleted after successful application.
