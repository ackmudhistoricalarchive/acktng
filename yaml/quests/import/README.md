# yaml/quests/import/

New quest templates to INSERT into the database. One file per quest.

Applied by:
    python3 tools/yaml_apply.py yaml/quests/import/200.yaml
    python3 tools/yaml_apply.py yaml/quests/import/   # all files in dir

The file is deleted after successful application.
