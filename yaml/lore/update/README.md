# yaml/lore/update/

Patches to existing lore topics. Include `filename` plus any fields to change.
If `entries` is present, it fully replaces all entries for the topic.

Applied by:
    python3 tools/yaml_apply.py yaml/lore/update/midgaard-history.yaml

The file is deleted after successful application.
