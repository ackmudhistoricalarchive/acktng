# yaml/help/update/

Patches to existing help entries. Include `filename` plus only the fields to change.

Applied by:
    python3 tools/yaml_apply.py yaml/help/update/berserk.yaml

The file is deleted after successful application.
